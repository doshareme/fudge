# fudge
Open-source distributed object storage built for reliability and fast operations

Run a local node quickly buy using `fudge serve`

## Installation

Windows
`choco install fudge`

Mac
`brew install fudge`

Ubunutu
`sudo apt install fudge`
