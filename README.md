# fudge
Open-source distributed object storage built for reliability and fast operations

Run an S3 compatible local file server node quickly buy using `fudge serve`

## Installation

Windows
`choco install fudge`

Mac
`brew install fudge`

Ubunutu
`sudo apt install fudge`
